<legend>{{#if content.id}}Edit{{else}}Create{{/if}} Contact</legend>

{{#with content}}
  <div class="control-group">
    <label class="control-label" for="firstName">First name</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="firstName" id="firstName" placeholder="First name"}}
    </div>
  </div>
  <div class="control-group">
    <label class="control-label" for="lastName">Last name</label>
    <div class="controls">
      {{view Ember.TextField valueBinding="lastName" id="lastName" placeholder="Last name"}}
    </div>
  </div>
{{/with}}
<div class="form-actions">
  {{#if content.id}}
    {{submitButton "Update"}}
  {{else}}
    {{submitButton "Create"}}
  {{/if}}
  <a class="btn" {{action cancelEdit}}>Cancel</a>
</div>
